import requestConfig from './REQUEST_CONFIG'

const config = {
    title: 'demo',
    themeColor: "#333",
    requestConfig,
    hashRouter: true,
    redirectQueryName: 'path',
}
export default config

